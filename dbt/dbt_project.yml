name: pro_data
version: '1.0.0'
config-version: 2

profile: pro_data

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  pro_data:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
